{
  "name": "0x2f",
  "version": "0.5.0",
  "description": "0x2F — manage engineering work, not AI sessions. A task-native wrapper around coding agents with a local-first Web UI.",
  "private": true,
  "type": "module",
  "bin": {
    "2f": "./src/cli.mjs"
  },
  "files": [
    "src"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/cli.mjs",
    "check": "find src test relay \\( -name '*.mjs' -o -name '*.js' \\) -exec node --check {} \\;",
    "test": "node --test \"test/*.test.mjs\""
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
